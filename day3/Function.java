package day3;

public class Function {

    // puclic = 접근 권한
    // void = 리턴(반환) 데이터 타입
    // func1 = 함수명(변수명과 같이 개발자가 원하는 이름으로 작명)
    // 소괄호 안에 있는 것들 아래 예시에선 tmp = 매개변수 : 함수가 동작할 때 외부에서 받아오는 값 보통 함수에서 해당 매개변수를 사용해서 처리함
    public  static  void func1(int tmp)
    {

    }

    public  static  int func1(int num_l, int num_r)
    {
        return num_l + num_r;
    }

    public static void main(String[] args) {
        // 함수
        // 코드 저장소로 개발자가 원하는 기능을 구현할 때 사용
        // 입력 -> 처리 -> 반환
        // 함수의 가장 큰 사용 목적은 코드의 재사용이다.

        // 함수 규칙
        // 1. 함수는 정의한 후 사용하여야 한다.
        // 2. 리턴타입, 함수명, 매개변수를 지정해야한다.
        // 3. 같은 이름의 함수를 2개 이상 만들지 않는다.
        // 4. 함수는 호출하여야 사용할 수 있다.
        // 5. 함수의 개수는 제한이 없다.


    }
}
